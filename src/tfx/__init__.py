"""tfx — diversified multi-market (FX/stock/crypto) trend-following trading platform.

The package is organized so the deterministic decision core is shared, byte-for-byte, between
the backtester and the live engine (backtest-live parity). v1 implements the `data` layer.
"""

__version__ = "0.1.0"
