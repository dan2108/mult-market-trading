# Normalize line endings to LF in the repo (and on checkout) for cross-platform determinism.
* text=auto eol=lf

# Data fixtures must be byte-identical on every clone.
*.csv text eol=lf

# Binary artifacts.
*.parquet binary
